const timeUnits = [
    "s",
    "seconds",
    "m",
    "minutes",
    "h",
    "hours",
    "d",
    "days",
    "w",
    "weeks",
    "mo",
    "months",
    "y",
    "year"
];

export function timeStringToDate(time: string): Date {
    let milliseconds = 0;
    for (let i = 0, { length } = time; i < length; i++) {}
}